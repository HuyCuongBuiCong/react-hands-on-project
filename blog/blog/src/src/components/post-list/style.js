import {makeStyles} from '@material-ui/core';

const useStyle = makeStyles( () => ({
	root : {
		marginTop: '20px'
	},
	cardMedia : {
		display : 'flex',
		flexDirection: 'row'
	}
}));
export default useStyle;